import { connect } from './modules/connect';
import { model } from './modules/model';
import { schema } from './modules/schema';

const neogm = {
  connect,
  schema,
  model,
};

export default neogm;
